import { createTheme } from "@codedoc/core/transport";

export const theme = /*#__PURE__*/ createTheme({
  light: {
    primary: "#007bff",
    // background: "white",
  },
  dark: {
    primary: "#007bff",
  },
});
